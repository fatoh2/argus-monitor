{
  "name": "@argus/adapter-sdk",
  "version": "1.0.0",
  "description": "Chain adapter interface and types for blockchain monitoring — Solana, EVM, and beyond",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "prebuild": "rimraf dist",
    "test": "jest --passWithNoTests",
    "lint": "eslint \"src/**/*.ts\" --ext .ts"
  },
  "keywords": [
    "blockchain",
    "chain-adapter",
    "solana",
    "evm",
    "monitoring"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "rimraf": "^6.1.3",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
